<?xml version="1.0" encoding="utf-8"?>
<manifest package="himanshuw.scriptorial_waddle"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light">
        <activity android:name="himanshuw.waddle.HomeActivity">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:label="@string/title_activity_profile"
            android:name="himanshuw.waddle.NavigationDrawerActivity"
            android:theme="@style/AppTheme.NoActionBar" />

        <activity android:name="himanshuw.waddle.publisher.PublishActivity"
            android:parentActivityName="himanshuw.waddle.HomeActivity"
        />
        <activity android:name="himanshuw.waddle.reviewer.ReviewerActivity"
            android:parentActivityName="himanshuw.waddle.HomeActivity"/>
    </application>

</manifest>